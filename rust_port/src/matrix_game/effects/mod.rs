//! Port of `MatrixGame/src/Effects/`.
//! Only `point_light` is ported so far; the other ~18 effect files
//! (explosion, flame, dust, ...) will land here one-to-one with the
//! original `MatrixEffect*.cpp` names.

pub mod marquee;
pub mod point_light;
pub mod selection;
pub mod weapon;
