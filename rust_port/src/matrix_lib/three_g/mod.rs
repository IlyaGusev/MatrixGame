//! Port of `MatrixLib/3G/` — VectorObject, Texture, CCamera, ShadowProj, ...
//! So far only VectorObject parsing and Texture (DXT/mipmap upload) are ported.

pub mod animation;
pub mod texture;
pub mod vector_object;
